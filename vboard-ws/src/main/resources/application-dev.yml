spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vboard_pins
elasticsearch:
  serverUri: http://localhost:9200

error:
  whitelabel:
    enabled: true
endpoints:
  enabled: true
  sensitive: false
management:
  security:
    enabled: false

com:
  vsct:
    vboard:
      hostname: localhost
uploads:
  providerPath: /tmp/
  wordpressImagePath: /tmp/
proxy:
  hostname:
emails:
  hostname: dummy
